<!-- Avatar — rendered from DB, visible to everyone -->
      <div style="width: 35px; height: 35px; min-width: 35px; min-height: 35px;" class="rounded-full overflow-hidden flex-shrink-0">
      <div class="relative w-full h-full">
  <% if user.mashit_layers.any? %>
    <% user.mashit_layers.each do |layer| %>
      <img src="<%= layer['image'] %>"
           alt="<%= layer['name'] %>"
           class="absolute inset-0 w-full h-full object-cover">
    <% end %>
  <% else %>
    <img src="<%= asset_path('mashit.png') %>"
         alt="Default avatar"
         class="w-full h-full object-cover">
  <% end %>
</div>
      </div>