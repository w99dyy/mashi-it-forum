<%= form_with(model: [@topic, @post], class: "contents") do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation" class="bg-red-500 px-3 py-2 font-medium rounded-md mt-3">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul class="list-disc ml-6">
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
      <%= form.text_field :title,
        required: true,
        minlength: 5,
        class: "w-full px-3 py-2 text-white border border-[#2a2a2a] rounded-md outline-none", 
        placeholder: "Enter a title for your post" %>
  </div>

  <!-- Tags field -->
<div class="field mb-4 container mx-auto">
  <%= form.label :tag_list, "Tags", class: "text-xl font-bold text-white block pb-2 pt-5 pl-2" %>
  <div class="flex flex-wrap gap-2">
  <% @available_tags.each do |tag| %>
  <label class="flex items-center gap-2 cursor-pointer">
      <%= check_box_tag "post[tag_list][]",
      tag.name,
      post.tag_list.include?(tag.name),
      id:"tag_#{tag.id}",
      class: "hidden peer" %>
      <span class="bg-[#2a2a2a] peer-checked:bg-[#de7200] text-white px-4 py-1 rounded-full text-sm cursor-pointer transition"> <%= tag.name %> </span>
        </label>
      <% end %>
  </div>
</div>

  <div class="field mb-4 container mx-auto">
      <%= form.rich_text_area :body, class: "border border-[#2a2a2a] rounded-md outline-none",
      placeholder: "Write your post content here... You can use markdown for formatting." %>
    </div>


  <div class="inline">
    <%= form.submit class: "w-full sm:w-auto rounded-md px-3.5 py-2.5 bg-[#ed5024] hover:bg-[#e0603c] text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
